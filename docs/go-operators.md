# Go 运算符：算术、比较、逻辑与位运算

> 知识点总结：掌握 Go 中算术、关系、逻辑、位运算与赋值运算符的语义；理解**整数除法**、**无隐式类型转换**、**短路求值**等语言特性，能写出正确的数值与布尔表达式。

---

## 1. 为什么需要了解这个

运算符是表达式的基础。新人写 Go 时经常在运算符上踩坑：

- 用 `7 / 3` 期望得到 `2.33`，实际得到 `2`（整数除法截断）
- 把 `int` 和 `float64` 直接相加，编译报错（Go 无隐式转换）
- 混淆 `&`（按位与）与 `&&`（逻辑与）
- 以为 `++` 是表达式，写成 `n = i++`（Go 中 `++`/`--` 只能是独立语句）
- 不理解 `&&`、`||` 的**短路（short-circuit）**行为，导致副作用重复执行

本篇建立在 [变量与常量](go-variables-types-and-constants.md) 之后——你已会基本类型与显式转换，现在学习如何用运算符组合它们。后续控制流、切片索引都会用到比较与算术运算。

---

## 2. 核心概念

### 2.1 运算符分类总览

| 类别 | 运算符 | 说明 |
|------|--------|------|
| 算术（arithmetic） | `+` `-` `*` `/` `%` | `+` 也可用于字符串拼接 |
| 关系（relational） | `==` `!=` `<` `>` `<=` `>=` | 结果为 `bool` |
| 逻辑（logical） | `&&` `\|\|` `!` | 仅用于布尔值；有短路 |
| 位（bitwise） | `&` `\|` `^` `&^` `<<` `>>` | 作用于整数类型 |
| 赋值（assignment） | `=` `+=` `-=` … | 复合赋值是简写 |
| 自增自减 | `++` `--` | **只能是独立语句**，不是表达式 |
| 取地址 / 解引用 | `&` `*` | 与指针配合，见 2.7 |

### 2.2 算术运算符

```go
a, b := 7, 3
a + b   // 10
a - b   // 4
a * b   // 21
a / b   // 2  ← 两个 int 相除，结果为 int，小数部分丢弃
a % b   // 1  ← 取余，仅整数类型
```

| 场景 | 行为 |
|------|------|
| 两个 `int` 相除 | 结果为 `int`，向零截断 |
| 需要小数 | 先显式转换：`float64(a) / float64(b)` |
| 字符串 `+` | 拼接，如 `"hello" + " " + "go"` |
| 不同类型数值运算 | **编译错误**，须先转换到同一类型 |

**无隐式转换示例：**

```go
var i int = 1
var f float64 = 1.5
// i + f  // 编译错误：invalid operation
float64(i) + f // OK
```

### 2.3 关系运算符

比较两个**同类型可比较**的值，返回 `bool`：

```go
x == y   // 相等
x != y   // 不等
x < y    x > y    x <= y    x >= y
```

| 可比较 | 不可直接 `==` 比较 |
|--------|-------------------|
| 基本类型、指针、数组、结构体（字段可比较时） | slice、map、function（只能用 `== nil`） |

slice 和 map 要用 `reflect.DeepEqual` 或逐元素比较，不能 `s1 == s2`。

### 2.4 逻辑运算符与短路求值

```go
ok && ready   // 两者都为 true 才 true
ok || backup  // 其一为 true 即 true
!ok           // 取反
```

**短路（short-circuit）：**

| 表达式 | 若左侧为 | 右侧 |
|--------|----------|------|
| `A && B` | `false` | **不执行** B |
| `A \|\| B` | `true` | **不执行** B |

```go
if user != nil && user.IsAdmin() { ... }
// user 为 nil 时不会调用 IsAdmin()，避免空指针 panic
```

### 2.5 位运算符

作用于**整数类型**的底层二进制位：

| 运算符 | 含义 | 示例 `12 & 10` |
|--------|------|----------------|
| `&` | 按位与 | `1100 & 1010` → `1000` → 8 |
| `\|` | 按位或 | `1100 \| 1010` → `1110` → 14 |
| `^` | 按位异或 | `1100 ^ 1010` → `0110` → 6 |
| `&^` | 按位清除（AND NOT） | Go 特有 |
| `<<` | 左移 | `1 << 3` → 8 |
| `>>` | 右移 | `8 >> 2` → 2 |

日常业务代码较少手写位运算；读权限常量（`iota` 左移）、协议解析时会遇到。

### 2.6 赋值与复合赋值

```go
n = 10
n += 3   // n = n + 3
n -= 1
n *= 2
n /= 2
n %= 3
```

复合赋值要求左右类型兼容；`n` 必须先声明。

### 2.7 自增、自减与指针运算符

**`++` 和 `--` 只能是独立语句：**

```go
i++      // OK
++i      // 语法错误：Go 没有前缀 ++
j = i++  // 语法错误：++ 不是表达式
```

**取地址 `&` 与解引用 `*`：**

```go
n := 42
p := &n   // p 是指向 int 的指针
*p = 100  // 通过指针修改 n
```

指针运算细节在后续专题展开；此处知道 `&` 取地址、`*` 解引用即可。

### 2.8 运算符优先级（简记）

从高到低（同一行优先级相近）：

1. `*` `/` `%` `<<` `>>` `&` `&^`
2. `+` `-` `|` `^`
3. `==` `!=` `<` `<=` `>` `>=`
4. `&&`
5. `||`

拿不准时用**括号**明确意图，可读性优先于省字符。

---

## 3. 动手实践

示例代码在 [`example/operators/`](../example/operators/)。

### 3.1 运行示例

```bash
cd example/operators
go run .                        # 全部演示
go run . -mode=arithmetic       # 算术与整数除法
go run . -mode=compare          # 关系运算
go run . -mode=logic            # 逻辑与短路
go run . -mode=bitwise          # 位运算
go run . -mode=assign           # 赋值、++、指针
```

预期（节选）：

```
--- 算术运算符 ---
7 + 3 = 10
7 / 3 = 2 (整数除法)
float64(7)/float64(3) = 2.33
--- 逻辑运算符 ---
ok && fail: false
```

### 3.2 跟着改：体会整数除法

在 `demoArithmetic` 中尝试：

```go
fmt.Println(5 / 2)              // 2
fmt.Println(5.0 / 2)            // 2.5（无类型常量参与运算）
fmt.Println(float64(5) / 2)     // 2.5
```

### 3.3 跟着改：短路避免 panic

```go
var p *int
// fmt.Println(*p != 0)          // panic：p 为 nil
if p != nil && *p != 0 {
    fmt.Println("非零")
}
```

### 3.4 自检清单

- [ ] 能说出 `7/3` 在 Go 中的结果及原因
- [ ] 知道 `int` 与 `float64` 不能直接相加
- [ ] 能区分 `&` 与 `&&`
- [ ] 知道 `++` 不能写在表达式里
- [ ] `go run .` 各 `-mode` 无报错

---

## 4. 常见坑与排查

### 4.1 整数除法丢失小数

```go
avg := sum / count // 两个 int → 截断
```

**修复：** `float64(sum) / float64(count)`，或全程用浮点类型。

### 4.2 混淆按位与与逻辑与

```go
if a & b > 0 { }   // 先算 a&b，再比较
if a && b { }      // 逻辑与，要求 a、b 为 bool
```

**修复：** 布尔条件用 `&&`；位掩码用 `&`。

### 4.3 对 slice 使用 `==`

```go
s1 := []int{1, 2}
s2 := []int{1, 2}
// s1 == s2  // 编译错误
```

**修复：** 用 `slices.Equal`（Go 1.21+）或 `reflect.DeepEqual`，或自己循环比较。

### 4.4 副作用在短路表达式中「有时不执行」

```go
if ok || doSomething() { } // ok 为 true 时 doSomething 不会被调用
```

**修复：** 若必须执行副作用，拆成独立语句，不要依赖 `||` 右侧。

---

## 5. 小结与延伸阅读

**要点回顾：**

1. Go **无隐式数值转换**，混合类型运算须显式 `float64(i)` 等
2. 两个整数 `/` 结果为整数，小数被截断
3. `&&`、`||` 会短路，常用于 `nil` 检查后再访问字段
4. `++`/`--` 只能是独立语句，不是表达式
5. slice、map 不能 `==` 比较内容（仅可与 `nil` 比）
6. 位运算 `&` `|` `^` `<<` `>>` 用于整数；与逻辑运算符不同

**官方文档：**

- [A Tour of Go：Operators](https://go.dev/tour/basics/11)
- [Go 语言规范：运算符](https://go.dev/ref/spec#Operators)

**与本仓库的关系：**

- 上一篇：[Go 变量、类型与常量](go-variables-types-and-constants.md)
- 示例：[`example/operators/`](../example/operators/)
- 下一篇：[Go 字符串操作](go-string-operations.md)
