### 作业：函数版的学生管理系统

### 作业：结构体版的学生管理系统

### 作业: 数据库版的学生管理系统

本地需安装并启动 MySQL。默认连接：`root:root@127.0.0.1:3306`，库名 `go_student`（首次运行自动建库建表）。

可在 `db.go` 改常量，或用环境变量：`MYSQL_USER` / `MYSQL_PASSWORD` / `MYSQL_HOST` / `MYSQL_DB`。

```bash
go run .
# 选择 3 进入数据库版
```
